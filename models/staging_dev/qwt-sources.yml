version: 2
sources:
  - name: qwt_raw
    database: "{{env_var('DBT_RAWDB_NAME','QWT_DEV')}}"
    schema: "{{env_var('DBT_RAWSCHEMA_NAME','RAW_DEV')}}"
    tables:
      - name: customers
      - name: raw_products
      - name: raw_orders
      - name: raw_orderdetails
      - name: raw_employee
      - name: raw_office 
      - name: raw_shipments
      - name: raw_suppliers
  
 